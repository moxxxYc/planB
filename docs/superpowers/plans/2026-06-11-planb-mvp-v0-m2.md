# PlanB MVP v0 M2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the M2 run-flow and economy skeleton from Guardian Contract through Battle 3 rest/result routing, while preserving the M1 machine-to-lane read.

**Architecture:** Add a typed run-session model that owns Guardian, Gold, Reward 1, Shop 1, Rest, and route state. Wrap the existing Battle 1 vertical in a run-session scene so Battle 1-3 can reuse the M1 machine/battle surface with per-battle labels and M2 modifiers. Add small Control-based screens for Guardian Contract, Reward 1, First Shop / Rest, and Result Routing.

**Tech Stack:** Godot 4.6.2 stable, GDScript, Compatibility renderer / `gl_compatibility`, current root verification via `bash tools/verify_godot.sh`, headless Godot verification scripts.

---

## Plan Status And Permission Boundary

This document is an implementation plan only. It does not authorize code changes by itself. The user must explicitly approve execution before M2 implementation starts.

Current inputs:

- `docs/gstack-artifacts/planb-mvp-v0-reset-handoff-20260611.zh.md`
- `docs/mvp-learning-checkpoints.md`
- `docs/rewards-economy.md`
- `docs/mvp-hive-loadout.md`
- `docs/enemy-rules.md`
- `docs/gdd.md`
- current active Godot project under `godot/`
- current verifier `bash tools/verify_godot.sh`

Hard boundary:

- Do not read or copy from `docs/archive/implementations/godot-mvp-v0-20260611/`.
- Do not restore old `mvp/`.
- Do not use archived `mvp/tools/verify_all.sh` or archived `mvp/tools/verify_godot.sh`.
- Do not implement M3 counter families as real systems.
- Do not implement Battle 4, Battle 5, Reward 2, Endpoint, Endpoint Prep, Telegraphed Sweep, or final full-result analysis.
- Do not build the complete entity-based battlefield in M2. M2 may keep the M1 simplified lane model.

## M2 Scope

### In Scope

- MUST: Main Menu starts the M2 run session, not the isolated M1 battle scene.
- MUST: Guardian Contract screen with `巢脉母` and `酸冠母`.
- MUST: Guardian selection is fixed for the run and visible in subsequent battle/HUD state.
- MUST: Battle 1 reuses the M1 vertical and routes to Reward 1 on victory or Result Routing on loss.
- MUST: Reward 1 screen with `Pool Pocket`, `Prime Charge`, and `Slot Primer`.
- MUST: Reward 1 cards show `warehouse -> component -> operation` and player read text.
- MUST: Reward 1 selection applies a real machine modifier visible in Battle 2.
- MUST: Battle 2 routes to First Shop / Rest on victory or Result Routing on loss.
- MUST: Gold follows M2 skeleton faucet: start 0, Battle 1 win +6, Battle 2 win +6, Battle 3 win +8.
- MUST: First Shop shows 3 machine items, displays price, role, warehouse, target component, and player read text.
- MUST: First Shop allows at most 1 neutral machine modifier purchase.
- MUST: Rest is separate from neutral modifier purchase cap, costs 3 Gold, restores 20 current Guardian HP, and appears only when Guardian HP is below max.
- MUST: Battle 3 exists as a routed battle node and can route to Battle 3 Rest or Result Routing.
- MUST: Result Routing records Guardian, Reward 1, Shop 1, Gold, Rest usage, last battle, and result.
- MUST: `bash tools/verify_godot.sh` runs M0, M1, and M2 verifiers.

### Out Of Scope

- Full `Pool Polluter`, `Echo Breaker`, and `Stagger Punisher` behavior. M3 owns this.
- Reward 2, Battle 4, Battle 5, Endpoint Prep, Endpoint, and final cause-tag result page. M4 owns the end state.
- Complete unit entity battlefield with movement, damage, death, Gate HP, and Guardian HP combat simulation. This needs a separate M3 prerequisite plan.
- Final balance, final art, final audio, final localization tables.

### Placeholder OK

- Guardian tactical/strategic effects may be represented as run markers in M2, as long as the Guardian card clearly states the axis tendency and the selected Guardian is visible in battle.
- Battle 3 counter pressure may be a labeled warning and simplified route result. The three counter families are not real until M3.
- Shop stocking can be deterministic in M2: show `Front Recycle`, `Surge Buffer`, and `Queue Brace`. The full adaptive stock priority arrives with M3 counters.

## File Structure

Create:

- `godot/scripts/data/guardian_definition.gd` - typed Guardian Contract data.
- `godot/scripts/data/modifier_definition.gd` - typed Reward / Shop / Rest modifier data.
- `godot/scripts/model/run/run_session_model.gd` - run state, route state, economy, selected definitions, and result record.
- `godot/scripts/run/mvp_run_session.gd` - top-level M2 screen controller.
- `godot/scripts/ui/run/guardian_contract_view.gd` - Guardian Contract UI.
- `godot/scripts/ui/run/reward_choice_view.gd` - Reward 1 UI.
- `godot/scripts/ui/run/shop_rest_view.gd` - First Shop / Rest UI.
- `godot/scripts/ui/run/run_result_view.gd` - Result Routing UI.
- `godot/scripts/ui/run/run_hud_view.gd` - small run-state HUD strip used above battles.
- `godot/scenes/run/mvp_run_session.tscn` - M2 run wrapper scene.
- `godot/tools/verify_m2_run_flow.gd` - headless M2 verification.

Modify:

- `godot/scripts/ui/main_menu.gd` - route Start to `res://scenes/run/mvp_run_session.tscn`.
- `godot/scripts/model/machine/machine_simulator.gd` - apply M2 modifier effects.
- `godot/scripts/ui/machine_board_view.gd` - expose dynamic Pool capacity and M2 modifier markers.
- `godot/scripts/ui/machine_strip_view.gd` - render selected reward/shop markers and dynamic Pool capacity.
- `godot/scripts/run/battle_one_vertical.gd` - accept battle number, run model, and M2 modifiers; expose route hooks for run wrapper.
- `godot/scenes/run/battle_one_vertical.tscn` - add small battle-complete signal path only if needed by the wrapper.
- `tools/verify_godot.sh` - run M2 verifier after M1 verifier.
- `docs/PROGRESS.md` - record M2 implementation status only after implementation passes.

Do not modify:

- `docs/archive/**`
- archived `mvp/**` rename targets
- gameplay formal docs, unless the user separately asks to change canon

## Design Decisions Locked By This Plan

1. M2 uses `godot/` as the active project.
2. M2 keeps `bash tools/verify_godot.sh` as the only current root verifier.
3. M2 extends the M1 battle surface instead of replacing it.
4. M2 makes Reward 1 and the first purchased shop item alter machine behavior, not only UI state.
5. M2 validates the front half of a run. It is allowed to stop at Battle 3 rest/result routing.

## Acceptance Criteria

### Engineering Done

- [ ] `bash tools/verify_godot.sh` prints `verify_project: PASS`, `verify_m1_machine_to_lane: PASS`, `verify_m2_run_flow: PASS`, and `verify_godot: PASS`.
- [ ] Main Menu Start opens the M2 run session.
- [ ] Headless verifier can select either Guardian and enter Battle 1.
- [ ] Headless verifier can force or advance Battle 1 victory, receive +6 Gold, and open Reward 1.
- [ ] Headless verifier can select each Reward 1 option and observe a real machine state change in Battle 2.
- [ ] Headless verifier can force or advance Battle 2 victory, reach 12 Gold, open First Shop / Rest, buy exactly 1 neutral modifier, and fail to buy a second neutral modifier.
- [ ] Headless verifier can buy Rest only when Guardian HP is below max; Rest costs 3 Gold and restores 20 current HP without increasing max HP.
- [ ] Headless verifier can route Battle 3 victory to Battle 3 Rest and Battle 3 loss to Result Routing.

### Design Done

- [ ] Guardian Contract reads as a run contract, not a controllable hero picker.
- [ ] Reward 1 cards read as `Launch / Tuning / Unit` machine-axis choices, not generic buffs.
- [ ] Battle 2 shows the selected Reward 1 marker on the machine/HUD and produces a visible machine difference.
- [ ] First Shop reads as machine patch / pivot / deepen, not a buy-everything stat shop.
- [ ] Rest reads as a Gold sink for Guardian HP pressure, not as a free safety button.

### Not Done Until

- [ ] A runtime-backed review confirms the M2 scene opens from Main Menu and GoPeak runtime addon connects.
- [ ] A visual pass confirms all player-facing explanatory text is Chinese except formal labels such as `Launch`, `Tuning`, `Unit`, `Queue`, `Gold`, and item names.
- [ ] M2 implementation does not introduce old `mvp/` verification routes.

## Task 1: Write The Failing M2 Verifier

**Files:**

- Create: `godot/tools/verify_m2_run_flow.gd`
- Modify: `tools/verify_godot.sh`

- [ ] **Step 1: Create the M2 verifier file**

Create `godot/tools/verify_m2_run_flow.gd` with the public API checks the implementation must satisfy:

```gdscript
extends SceneTree

const RUN_SCENE_PATH: String = "res://scenes/run/mvp_run_session.tscn"

func _initialize() -> void:
	var failed := false
	var scene: PackedScene = load(RUN_SCENE_PATH)
	if scene == null:
		push_error("M2 run session scene missing: %s" % RUN_SCENE_PATH)
		quit(1)
		return

	var run: Node = scene.instantiate()
	root.add_child(run)

	failed = not _verify_guardian_contract(run) or failed
	failed = not _verify_reward_one_and_battle_two(run) or failed
	failed = not _verify_shop_rest_and_battle_three(run) or failed
	failed = not _verify_result_routing(run) or failed

	root.remove_child(run)
	run.free()

	if failed:
		quit(1)
		return

	print("verify_m2_run_flow: PASS")
	quit(0)

func _verify_guardian_contract(run: Node) -> bool:
	if not _has_methods(run, [
		"get_current_node_id",
		"get_guardian_card_text",
		"select_guardian",
		"confirm_guardian",
		"get_selected_guardian_id",
	]):
		return false

	if String(run.call("get_current_node_id")) != "guardian_contract":
		push_error("M2 run should start at Guardian Contract.")
		return false

	var vein_text := String(run.call("get_guardian_card_text", "hive_vein_mother"))
	if not vein_text.contains("巢脉母") or not vein_text.contains("Launch") or not vein_text.contains("契约"):
		push_error("巢脉母 card must show name, Launch tendency, and contract framing.")
		return false

	var acid_text := String(run.call("get_guardian_card_text", "hive_acid_crown_mother"))
	if not acid_text.contains("酸冠母") or not acid_text.contains("Tuning") or not acid_text.contains("契约"):
		push_error("酸冠母 card must show name, Tuning tendency, and contract framing.")
		return false

	run.call("select_guardian", "hive_vein_mother")
	run.call("confirm_guardian")

	if String(run.call("get_selected_guardian_id")) != "hive_vein_mother":
		push_error("Guardian selection did not persist into the run model.")
		return false

	if String(run.call("get_current_node_id")) != "battle_1":
		push_error("Guardian confirmation should route to Battle 1.")
		return false

	return true

func _verify_reward_one_and_battle_two(run: Node) -> bool:
	if not _has_methods(run, [
		"complete_current_battle_for_verifier",
		"get_gold",
		"get_reward_card_text",
		"choose_reward_one",
		"get_reward_one_id",
		"get_battle_modifier_marker_text",
	]):
		return false

	run.call("complete_current_battle_for_verifier", "Win")
	if String(run.call("get_current_node_id")) != "reward_1":
		push_error("Battle 1 win should route to Reward 1.")
		return false
	if int(run.call("get_gold")) != 6:
		push_error("Battle 1 win should add 6 Gold.")
		return false

	var reward_text := String(run.call("get_reward_card_text", "prime_charge"))
	if not reward_text.contains("Tuning") or not reward_text.contains("Prime") or not reward_text.contains("value +1 -> +2"):
		push_error("Prime Charge card must show axis, component, and operation.")
		return false

	run.call("choose_reward_one", "prime_charge")
	if String(run.call("get_reward_one_id")) != "prime_charge":
		push_error("Reward 1 choice did not persist.")
		return false
	if String(run.call("get_current_node_id")) != "battle_2":
		push_error("Reward 1 choice should route to Battle 2.")
		return false

	var marker_text := String(run.call("get_battle_modifier_marker_text"))
	if not marker_text.contains("Prime Charge") or not marker_text.contains("Tuning"):
		push_error("Battle 2 should expose the selected Reward 1 machine marker.")
		return false

	return true

func _verify_shop_rest_and_battle_three(run: Node) -> bool:
	if not _has_methods(run, [
		"get_shop_card_text",
		"buy_shop_item",
		"get_shop_purchase_id",
		"damage_guardian_for_verifier",
		"can_buy_rest",
		"buy_rest",
		"get_guardian_hp",
		"get_guardian_max_hp",
		"get_rest_count",
	]):
		return false

	run.call("complete_current_battle_for_verifier", "Win")
	if String(run.call("get_current_node_id")) != "shop_1":
		push_error("Battle 2 win should route to First Shop / Rest.")
		return false
	if int(run.call("get_gold")) != 12:
		push_error("Battle 2 win should bring Gold to 12 after two wins.")
		return false

	var shop_text := String(run.call("get_shop_card_text", "surge_buffer"))
	if not shop_text.contains("4 Gold") or not shop_text.contains("Patch") or not shop_text.contains("Tuning.Surge"):
		push_error("Shop card must show price, role, and machine component.")
		return false

	run.call("buy_shop_item", "surge_buffer")
	if String(run.call("get_shop_purchase_id")) != "surge_buffer":
		push_error("Shop purchase did not persist.")
		return false
	if int(run.call("get_gold")) != 8:
		push_error("Surge Buffer should cost 4 Gold from 12.")
		return false

	run.call("buy_shop_item", "queue_brace")
	if String(run.call("get_shop_purchase_id")) != "surge_buffer":
		push_error("First Shop should allow only one neutral machine modifier.")
		return false

	if bool(run.call("can_buy_rest")):
		push_error("Rest should not be available while Guardian HP is full.")
		return false

	run.call("damage_guardian_for_verifier", 30)
	if not bool(run.call("can_buy_rest")):
		push_error("Rest should become available after Guardian HP damage.")
		return false
	run.call("buy_rest")
	if int(run.call("get_gold")) != 5:
		push_error("Rest should cost 3 Gold.")
		return false
	if int(run.call("get_guardian_hp")) != mini(int(run.call("get_guardian_max_hp")), 90):
		push_error("Rest should restore 20 current HP without exceeding max HP.")
		return false
	if int(run.call("get_rest_count")) != 1:
		push_error("First Shop rest count should be 1 after buying rest.")
		return false

	run.call("confirm_shop_and_rest")
	if String(run.call("get_current_node_id")) != "battle_3":
		push_error("Leaving First Shop should route to Battle 3.")
		return false

	return true

func _verify_result_routing(run: Node) -> bool:
	if not _has_methods(run, [
		"get_result_summary_text",
		"get_result_record",
	]):
		return false

	run.call("complete_current_battle_for_verifier", "Loss")
	if String(run.call("get_current_node_id")) != "result_routing":
		push_error("Battle 3 loss should route to Result Routing.")
		return false

	var result_text := String(run.call("get_result_summary_text"))
	if not result_text.contains("巢脉母") or not result_text.contains("Prime Charge") or not result_text.contains("Surge Buffer"):
		push_error("Result Routing must summarize Guardian, Reward 1, and Shop 1.")
		return false

	var record: Dictionary = run.call("get_result_record")
	if String(record.get("last_battle", "")) != "battle_3":
		push_error("Result record should include last_battle=battle_3.")
		return false
	if String(record.get("battle_result", "")) != "Loss":
		push_error("Result record should include battle_result=Loss.")
		return false

	return true

func _has_methods(node: Node, methods: Array[String]) -> bool:
	for method_name: String in methods:
		if not node.has_method(method_name):
			push_error("M2 run scene missing method: %s" % method_name)
			return false
	return true
```

- [ ] **Step 2: Add the M2 verifier to `tools/verify_godot.sh`**

Add this block after the M1 verifier block:

```bash
if [[ ! -f "$PROJECT_DIR/tools/verify_m2_run_flow.gd" ]]; then
  echo "Missing required M2 verifier at $PROJECT_DIR/tools/verify_m2_run_flow.gd" >&2
  exit 1
fi

run_godot_verifier "res://tools/verify_m2_run_flow.gd"
```

- [ ] **Step 3: Run the verifier and confirm it fails for the right reason**

Run:

```bash
bash tools/verify_godot.sh
```

Expected before implementation:

```text
verify_project: PASS
verify_m1_machine_to_lane: PASS
M2 run session scene missing: res://scenes/run/mvp_run_session.tscn
verify_godot: FAIL
```

Do not continue if M0 or M1 fails.

## Task 2: Add M2 Data Definitions And Run Model

**Files:**

- Create: `godot/scripts/data/guardian_definition.gd`
- Create: `godot/scripts/data/modifier_definition.gd`
- Create: `godot/scripts/model/run/run_session_model.gd`

- [ ] **Step 1: Create `GuardianDefinition`**

```gdscript
class_name GuardianDefinition
extends Resource

@export var id: String = ""
@export var display_name: String = ""
@export var axis: String = ""
@export_multiline var tactical_read: String = ""
@export_multiline var strategic_read: String = ""
@export_multiline var risk_read: String = ""

func to_card_text() -> String:
	return "%s\n契约倾向：%s\n战术防守：%s\n机器契约：%s\n风险：%s" % [
		display_name,
		axis,
		tactical_read,
		strategic_read,
		risk_read,
	]
```

- [ ] **Step 2: Create `ModifierDefinition`**

```gdscript
class_name ModifierDefinition
extends Resource

enum SourceType { REWARD, SHOP, REST }

@export var id: String = ""
@export var display_name: String = ""
@export var source_type: SourceType = SourceType.REWARD
@export var warehouse: String = ""
@export var target_component: String = ""
@export var operation: String = ""
@export var role: String = ""
@export var gold_cost: int = 0
@export_multiline var player_read: String = ""

func to_card_text() -> String:
	var cost_text := "" if gold_cost <= 0 else "%d Gold\n" % gold_cost
	return "%s%s\n%s -> %s -> %s\n%s\n%s" % [
		cost_text,
		display_name,
		warehouse,
		target_component,
		operation,
		role,
		player_read,
	]
```

- [ ] **Step 3: Create `RunSessionModel`**

Create `godot/scripts/model/run/run_session_model.gd` with:

```gdscript
class_name RunSessionModel
extends RefCounted

const NODE_GUARDIAN_CONTRACT: String = "guardian_contract"
const NODE_BATTLE_1: String = "battle_1"
const NODE_REWARD_1: String = "reward_1"
const NODE_BATTLE_2: String = "battle_2"
const NODE_SHOP_1: String = "shop_1"
const NODE_BATTLE_3: String = "battle_3"
const NODE_REST_AFTER_BATTLE_3: String = "rest_after_battle_3"
const NODE_RESULT_ROUTING: String = "result_routing"

const RESULT_WIN: String = "Win"
const RESULT_LOSS: String = "Loss"

var current_node_id: String = NODE_GUARDIAN_CONTRACT
var selected_guardian_id: String = ""
var reward_one_id: String = ""
var shop_purchase_id: String = ""
var gold: int = 0
var guardian_max_hp: int = 100
var guardian_hp: int = 100
var first_shop_rest_count: int = 0
var battle_three_rest_count: int = 0
var last_battle: String = ""
var last_battle_result: String = ""
var result_record: Dictionary = {}

func select_guardian(guardian_id: String) -> void:
	selected_guardian_id = guardian_id

func confirm_guardian() -> void:
	if selected_guardian_id.is_empty():
		push_error("Cannot confirm Guardian before selection.")
		return
	current_node_id = NODE_BATTLE_1

func complete_battle(result: String) -> void:
	last_battle = current_node_id
	last_battle_result = result
	if result == RESULT_LOSS:
		_route_to_result()
		return

	match current_node_id:
		NODE_BATTLE_1:
			gold += 6
			current_node_id = NODE_REWARD_1
		NODE_BATTLE_2:
			gold += 6
			current_node_id = NODE_SHOP_1
		NODE_BATTLE_3:
			gold += 8
			current_node_id = NODE_REST_AFTER_BATTLE_3
		_:
			push_error("Cannot complete battle from node: %s" % current_node_id)

func choose_reward_one(modifier_id: String) -> void:
	if current_node_id != NODE_REWARD_1:
		push_error("Reward 1 can only be selected from reward_1.")
		return
	reward_one_id = modifier_id
	current_node_id = NODE_BATTLE_2

func buy_shop_item(modifier_id: String, cost: int) -> void:
	if current_node_id != NODE_SHOP_1:
		push_error("Shop item can only be bought in shop_1.")
		return
	if not shop_purchase_id.is_empty():
		return
	if gold < cost:
		return
	shop_purchase_id = modifier_id
	gold -= cost

func damage_guardian(amount: int) -> void:
	guardian_hp = maxi(0, guardian_hp - amount)

func can_buy_rest() -> bool:
	return guardian_hp < guardian_max_hp and gold >= 3 and _rest_limit_remaining() > 0

func buy_rest() -> void:
	if not can_buy_rest():
		return
	gold -= 3
	guardian_hp = mini(guardian_max_hp, guardian_hp + 20)
	if current_node_id == NODE_SHOP_1:
		first_shop_rest_count += 1
	elif current_node_id == NODE_REST_AFTER_BATTLE_3:
		battle_three_rest_count += 1

func confirm_shop_and_rest() -> void:
	if current_node_id != NODE_SHOP_1:
		push_error("Can only leave first shop from shop_1.")
		return
	current_node_id = NODE_BATTLE_3

func confirm_battle_three_rest() -> void:
	if current_node_id != NODE_REST_AFTER_BATTLE_3:
		push_error("Can only leave Battle 3 rest from rest_after_battle_3.")
		return
	_route_to_result()

func get_rest_count() -> int:
	return first_shop_rest_count if current_node_id == NODE_SHOP_1 else battle_three_rest_count

func _rest_limit_remaining() -> int:
	if current_node_id == NODE_SHOP_1:
		return 1 - first_shop_rest_count
	if current_node_id == NODE_REST_AFTER_BATTLE_3:
		return 1 - battle_three_rest_count
	return 0

func _route_to_result() -> void:
	current_node_id = NODE_RESULT_ROUTING
	result_record = {
		"guardian_id": selected_guardian_id,
		"reward1_id": reward_one_id,
		"shop1_purchase_id": shop_purchase_id,
		"gold": gold,
		"rest_first_shop": first_shop_rest_count,
		"rest_battle_three": battle_three_rest_count,
		"last_battle": last_battle,
		"battle_result": last_battle_result,
	}
```

- [ ] **Step 4: Run the M2 verifier**

Run:

```bash
bash tools/verify_godot.sh
```

Expected: still FAIL because the M2 scene/controller does not exist yet.

## Task 3: Add Machine Modifier Support

**Files:**

- Modify: `godot/scripts/model/machine/machine_simulator.gd`
- Modify: `godot/scripts/ui/machine_board_view.gd`
- Modify: `godot/scripts/ui/machine_strip_view.gd`

- [ ] **Step 1: Extend `MachineSimulator` state**

Add these fields:

```gdscript
var pool_capacity: int = 5
var prime_value_bonus: int = 1
var slot_progress_floor: Dictionary = {}
var front_recycle_enabled: bool = false
var surge_buffer_enabled: bool = false
var queue_brace_enabled: bool = false
var active_modifier_ids: Array[String] = []
```

- [ ] **Step 2: Add modifier application API**

Add:

```gdscript
func apply_modifier(modifier_id: String, payload: Dictionary = {}) -> void:
	if active_modifier_ids.has(modifier_id):
		return
	active_modifier_ids.append(modifier_id)
	match modifier_id:
		"pool_pocket":
			pool_capacity = mini(6, pool_capacity + 1)
			event_log.append("Modifier:Pool Pocket Launch.Pool.capacity +1")
		"prime_charge":
			prime_value_bonus = 2
			event_log.append("Modifier:Prime Charge Tuning.Prime.value_bonus +2")
		"slot_primer":
			var slot_id: int = int(payload.get("slot_id", 1))
			slot_progress_floor[slot_id] = 1
			slot_progress[slot_id] = maxi(int(slot_progress.get(slot_id, 0)), 1)
			event_log.append("Modifier:Slot Primer Unit.Slot.%d floor=1" % slot_id)
		"front_recycle":
			front_recycle_enabled = true
			event_log.append("Modifier:Front Recycle Launch.Recycle.return_position front")
		"surge_buffer":
			surge_buffer_enabled = true
			event_log.append("Modifier:Surge Buffer Tuning.Surge.charge_buffer on")
		"queue_brace":
			queue_brace_enabled = true
			event_log.append("Modifier:Queue Brace Unit.Queue.empty_gap_response on")
		_:
			push_error("Unknown machine modifier: %s" % modifier_id)
```

- [ ] **Step 3: Use dynamic Pool capacity and Prime bonus**

Change `_add_pool_ball` to check `pool_capacity` instead of literal `5`.

Change Prime handling from:

```gdscript
if tuning_result == "Prime":
	value += 1
```

to:

```gdscript
if tuning_result == "Prime":
	value += prime_value_bonus
```

- [ ] **Step 4: Preserve Slot Primer floor after entry generation**

After subtracting slot requirement in `_apply_unit_hit`, clamp to floor:

```gdscript
var floor_value: int = int(slot_progress_floor.get(slot_id, 0))
slot_progress[slot_id] = maxi(int(slot_progress[slot_id]), floor_value)
```

- [ ] **Step 5: Add public marker summaries for UI and verifier**

Add:

```gdscript
func get_modifier_marker_text() -> String:
	if active_modifier_ids.is_empty():
		return "本局机器修正：无"
	return "本局机器修正：%s" % ", ".join(active_modifier_ids)

func get_pool_capacity() -> int:
	return pool_capacity
```

- [ ] **Step 6: Update machine UI to read dynamic Pool capacity**

Replace hard-coded Pool capacity display in machine board and strip with `machine.get_pool_capacity()`. The visible Pool row must show 6 slots after `Pool Pocket`.

- [ ] **Step 7: Run verifier**

Run:

```bash
bash tools/verify_godot.sh
```

Expected: M1 still PASS. M2 still FAIL until run session UI exists.

## Task 4: Add M2 Run Session Scene And Controller

**Files:**

- Create: `godot/scenes/run/mvp_run_session.tscn`
- Create: `godot/scripts/run/mvp_run_session.gd`
- Modify: `godot/scripts/ui/main_menu.gd`

- [ ] **Step 1: Create `mvp_run_session.gd` controller**

The script must own:

```gdscript
class_name MvpRunSession
extends Control

const BattleScene: PackedScene = preload("res://scenes/run/battle_one_vertical.tscn")

var session: RunSessionModel = RunSessionModel.new()
var selected_guardian_id: String = ""
var current_battle: Node = null

var guardians: Dictionary = {}
var rewards: Dictionary = {}
var shop_items: Dictionary = {}

@onready var screen_slot: Control = %ScreenSlot
@onready var hud_slot: Control = %HudSlot
```

It must expose every method required by `verify_m2_run_flow.gd`.

- [ ] **Step 2: Define in-code M2 catalogs**

Use typed data constructors in `_build_catalogs()`:

```gdscript
func _build_catalogs() -> void:
	guardians = {
		"hive_vein_mother": _guardian("hive_vein_mother", "巢脉母", "Launch", "低频牵缚入侵者", "Recycle 偶尔多返回 clean ball", "帮助稳定补线，但不能替代奖励和商店"),
		"hive_acid_crown_mother": _guardian("hive_acid_crown_mother", "酸冠母", "Tuning", "受击后低频酸液反喷", "Gate miss 计数后推成 Prime", "允许有限卖血，但真实消耗 HP"),
	}
	rewards = {
		"pool_pocket": _modifier("pool_pocket", "Pool Pocket", ModifierDefinition.SourceType.REWARD, "Launch", "Launch.Pool.capacity", "+1", "Reward", 0, "Pool 多一个可见槽位，断档更少。"),
		"prime_charge": _modifier("prime_charge", "Prime Charge", ModifierDefinition.SourceType.REWARD, "Tuning", "Tuning.Prime.value_bonus", "value +1 -> +2", "Reward", 0, "Prime 命中时 Unit 进度跳得更明显。"),
		"slot_primer": _modifier("slot_primer", "Slot Primer", ModifierDefinition.SourceType.REWARD, "Unit", "Unit.Slot.progress_floor", "选择 1 个 slot，floor = 1", "Reward", 0, "一个 Unit 槽整局领先一步。"),
	}
	shop_items = {
		"front_recycle": _modifier("front_recycle", "Front Recycle", ModifierDefinition.SourceType.SHOP, "Launch", "Launch.Recycle.return_position", "front insert", "Pivot", 5, "miss 回流更快转成下一轮压力。"),
		"surge_buffer": _modifier("surge_buffer", "Surge Buffer", ModifierDefinition.SourceType.SHOP, "Tuning", "Tuning.Surge.charge_buffer", "store 1 charge", "Patch", 4, "Surge 未成兵时把部署节奏价值留到下一次。"),
		"queue_brace": _modifier("queue_brace", "Queue Brace", ModifierDefinition.SourceType.SHOP, "Unit", "Unit.Queue.empty_gap_response", "+1 progress on long gap", "Patch", 4, "队列空档时补一个低阶进度，避免长时间无部署。"),
	}
```

The helper methods `_guardian()` and `_modifier()` must return typed `GuardianDefinition` and `ModifierDefinition` instances.

- [ ] **Step 3: Create scene shell**

Create `godot/scenes/run/mvp_run_session.tscn` with:

```text
MvpRunSession (Control, script MvpRunSession)
  Background (ColorRect)
  SafeArea (MarginContainer)
    RootRows (VBoxContainer)
      HudSlot (VBoxContainer, unique name)
      ScreenSlot (Control, unique name)
```

Use full-rect anchors and existing dark war-table palette.

- [ ] **Step 4: Route Main Menu to M2 scene**

Change `godot/scripts/ui/main_menu.gd`:

```gdscript
const M2_RUN_SESSION: String = "res://scenes/run/mvp_run_session.tscn"

func _on_start_button_pressed() -> void:
	get_tree().change_scene_to_file(M2_RUN_SESSION)
```

- [ ] **Step 5: Run verifier**

Run:

```bash
bash tools/verify_godot.sh
```

Expected: M2 fails on missing UI behavior until the views exist.

## Task 5: Add Guardian Contract View

**Files:**

- Create: `godot/scripts/ui/run/guardian_contract_view.gd`
- Modify: `godot/scripts/run/mvp_run_session.gd`

- [ ] **Step 1: Create the view script**

The view is a `VBoxContainer` with two buttons and one confirm button. It emits:

```gdscript
signal guardian_selected(guardian_id: String)
signal guardian_confirmed
```

It must expose:

```gdscript
func render(guardian_defs: Dictionary, selected_id: String) -> void
func get_card_text(guardian_id: String) -> String
```

- [ ] **Step 2: Wire MvpRunSession public methods**

Implement:

```gdscript
func get_guardian_card_text(guardian_id: String) -> String:
	return guardian_view.get_card_text(guardian_id)

func select_guardian(guardian_id: String) -> void:
	session.select_guardian(guardian_id)
	_render_current_node()

func confirm_guardian() -> void:
	session.confirm_guardian()
	_render_current_node()

func get_selected_guardian_id() -> String:
	return session.selected_guardian_id
```

- [ ] **Step 3: Run verifier**

Run:

```bash
bash tools/verify_godot.sh
```

Expected: Guardian Contract checks pass; later M2 checks fail.

## Task 6: Add Reward 1 Flow And Battle 2 Modifier Marker

**Files:**

- Create: `godot/scripts/ui/run/reward_choice_view.gd`
- Create: `godot/scripts/ui/run/run_hud_view.gd`
- Modify: `godot/scripts/run/mvp_run_session.gd`
- Modify: `godot/scripts/run/battle_one_vertical.gd`

- [ ] **Step 1: Add Reward Choice View**

The view displays `Pool Pocket`, `Prime Charge`, and `Slot Primer`. It emits:

```gdscript
signal reward_chosen(modifier_id: String)
```

It must expose:

```gdscript
func render(reward_defs: Dictionary) -> void
func get_card_text(modifier_id: String) -> String
```

- [ ] **Step 2: Add run HUD view**

HUD text must include:

```text
Guardian: 巢脉母 | Gold: 6 | Reward 1: Prime Charge | Shop 1: 无
```

Use Chinese labels:

```text
守护者：巢脉母 | Gold：6 | 第一次奖励：Prime Charge | 第一次商店：无
```

- [ ] **Step 3: Add battle configuration API**

In `BattleOneVertical`, add:

```gdscript
var battle_number: int = 1
var run_session: RunSessionModel = null
var m2_modifier_payload: Dictionary = {}

func configure_for_run(p_battle_number: int, p_session: RunSessionModel, payload: Dictionary = {}) -> void:
	battle_number = p_battle_number
	run_session = p_session
	m2_modifier_payload = payload
	_apply_run_modifiers()
	_render()

func get_battle_modifier_marker_text() -> String:
	return machine.get_modifier_marker_text()
```

`_apply_run_modifiers()` must apply `reward_one_id` and `shop_purchase_id` to `machine`.

- [ ] **Step 4: Add verifier battle completion hook**

In `MvpRunSession`, implement:

```gdscript
func complete_current_battle_for_verifier(result: String) -> void:
	session.complete_battle(result)
	_render_current_node()
```

This hook is verifier-only and must not appear as a player button.

- [ ] **Step 5: Wire Reward 1 selection**

Implement:

```gdscript
func choose_reward_one(modifier_id: String) -> void:
	session.choose_reward_one(modifier_id)
	_render_current_node()

func get_reward_card_text(modifier_id: String) -> String:
	return reward_view.get_card_text(modifier_id)

func get_reward_one_id() -> String:
	return session.reward_one_id
```

- [ ] **Step 6: Run verifier**

Run:

```bash
bash tools/verify_godot.sh
```

Expected: Reward/Battle 2 checks pass; shop/rest checks fail until Task 7.

## Task 7: Add First Shop / Rest

**Files:**

- Create: `godot/scripts/ui/run/shop_rest_view.gd`
- Modify: `godot/scripts/run/mvp_run_session.gd`

- [ ] **Step 1: Create Shop / Rest view**

The view displays three deterministic shop items:

- `Front Recycle`, 5 Gold, `Pivot`, `Launch.Recycle.return_position`
- `Surge Buffer`, 4 Gold, `Patch`, `Tuning.Surge.charge_buffer`
- `Queue Brace`, 4 Gold, `Patch`, `Unit.Queue.empty_gap_response`

It emits:

```gdscript
signal shop_item_bought(modifier_id: String)
signal rest_bought
signal shop_confirmed
```

It must expose:

```gdscript
func render(shop_defs: Dictionary, session: RunSessionModel) -> void
func get_card_text(modifier_id: String) -> String
```

- [ ] **Step 2: Add shop public API**

Implement:

```gdscript
func get_shop_card_text(modifier_id: String) -> String:
	return shop_view.get_card_text(modifier_id)

func buy_shop_item(modifier_id: String) -> void:
	var def: ModifierDefinition = shop_items.get(modifier_id)
	if def == null:
		push_error("Unknown shop item: %s" % modifier_id)
		return
	session.buy_shop_item(modifier_id, def.gold_cost)
	_render_current_node()

func get_shop_purchase_id() -> String:
	return session.shop_purchase_id

func confirm_shop_and_rest() -> void:
	session.confirm_shop_and_rest()
	_render_current_node()
```

- [ ] **Step 3: Add rest public API**

Implement:

```gdscript
func damage_guardian_for_verifier(amount: int) -> void:
	session.damage_guardian(amount)
	_render_current_node()

func can_buy_rest() -> bool:
	return session.can_buy_rest()

func buy_rest() -> void:
	session.buy_rest()
	_render_current_node()

func get_guardian_hp() -> int:
	return session.guardian_hp

func get_guardian_max_hp() -> int:
	return session.guardian_max_hp

func get_rest_count() -> int:
	return session.get_rest_count()
```

- [ ] **Step 4: Run verifier**

Run:

```bash
bash tools/verify_godot.sh
```

Expected: Shop/Rest checks pass; result routing checks fail until Task 8.

## Task 8: Add Battle 3 Rest And Result Routing

**Files:**

- Create: `godot/scripts/ui/run/run_result_view.gd`
- Modify: `godot/scripts/run/mvp_run_session.gd`

- [ ] **Step 1: Create Result Routing view**

It must display:

```text
本次短局记录
守护者：巢脉母
第一次奖励：Prime Charge
第一次商店：Surge Buffer
Gold：5
休整：第一次商店 1 次 / Battle 3 后 0 次
最后节点：battle_3
结果：Loss
下一步：M3 将加入真实反制与 Battle 4-5
```

Expose:

```gdscript
func render(session: RunSessionModel, guardians: Dictionary, rewards: Dictionary, shop_items: Dictionary) -> void
func get_summary_text() -> String
```

- [ ] **Step 2: Wire result methods**

Implement:

```gdscript
func get_result_summary_text() -> String:
	return result_view.get_summary_text()

func get_result_record() -> Dictionary:
	return session.result_record.duplicate(true)
```

- [ ] **Step 3: Render Battle 3 rest node**

When `session.current_node_id == RunSessionModel.NODE_REST_AFTER_BATTLE_3`, reuse `ShopRestView` in rest-only mode:

- hide neutral machine shop item buttons
- show current Gold
- show Rest if Guardian HP is below max and Gold >= 3
- show continue button to result routing

- [ ] **Step 4: Run verifier**

Run:

```bash
bash tools/verify_godot.sh
```

Expected:

```text
verify_project: PASS
verify_m1_machine_to_lane: PASS
verify_m2_run_flow: PASS
verify_godot: PASS
```

## Task 9: Runtime Review And Documentation Update

**Files:**

- Modify: `docs/PROGRESS.md`

- [ ] **Step 1: Run GoPeak runtime check**

Start the active scene through GoPeak or Godot editor run:

```text
res://scenes/run/mvp_run_session.tscn
```

Pass criteria:

- Runtime addon connects on port 7777.
- Scene starts at Guardian Contract.
- Main Menu Start reaches the same run session.
- No GDScript errors.

- [ ] **Step 2: Manual visual checklist**

Check these screens:

- Guardian Contract
- Battle 1
- Reward 1
- Battle 2 with Reward marker
- First Shop / Rest
- Battle 3
- Result Routing

Pass criteria:

- Player-facing explanatory text is Chinese.
- Formal labels may remain English: `Launch`, `Tuning`, `Unit`, `Queue`, `Gold`, item names.
- No old debug log text is visible as player explanation.
- No screen says `mvp/` or archived verifier.

- [ ] **Step 3: Update `docs/PROGRESS.md`**

Add one 2026-06-11 entry after implementation passes:

```markdown
- M2 run-flow and economy skeleton implemented in `godot/`: Guardian Contract, Reward 1, Battle 2, First Shop / Rest, Battle 3 rest/result routing, Gold faucet, and M2 verifier. Current verifier remains `bash tools/verify_godot.sh`.
```

- [ ] **Step 4: Final verification**

Run:

```bash
bash tools/verify_godot.sh
```

Expected:

```text
verify_godot: PASS
```

## Self-Review

Spec coverage:

- M2 Guardian Contract: Task 5.
- Reward 1: Task 6.
- Battle 2: Task 6, using battle configuration and modifier markers.
- First Shop / Rest: Task 7.
- Battle 3: Task 8, using routed battle node and rest/result routing.
- Rest window: Task 7 and Task 8.
- Result routing: Task 8.
- Current verifier route: Task 1 and Task 9.

Scope guard:

- M3 counter families are not implemented.
- Reward 2, Battle 4-5, Endpoint, and Final Result are not implemented.
- Complete entity-based battlefield is not implemented in M2.

Placeholder scan:

- No task depends on unspecified files.
- No task uses archived `mvp/` routes.
- All M2 public APIs required by verifier are named in the plan.

Execution options after user confirmation:

1. Subagent-Driven: use a fresh subagent per task, review between tasks.
2. Inline Execution: execute tasks in this session with checkpoints after model, UI, and verifier.
