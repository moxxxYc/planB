{
  "name": "ball-machine-auto-battle-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "probe": "node scripts/run-build-probes.mjs",
    "audit:mvp": "node scripts/audit-mvp-readiness.mjs",
    "audit:v1.2": "node scripts/audit-v12-readiness.mjs",
    "smoke:build-visuals": "node scripts/capture-build-visuals.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
